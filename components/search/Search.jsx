import React from 'react';
import {Box} from "@mui/material";

function Search({name}) {
    console.log(name)
    return (
        <Box>
            search
        </Box>
    );
}

export default Search;
